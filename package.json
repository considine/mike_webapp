{
  "name": "mike",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "test": "mocha",
    "stop_mongo": "ps aux | grep mongo | grep -v grep | awk {'print $2'} | xargs kill -9",
    "start_mongo": "mongod &",
    "stop_node": "ps aux | grep node | grep -v grep | grep -v Atom | awk {'print $2'} | xargs kill -9",
    "start_node": "nodemon &",
    "restart_everything": "npm run stop_mongo && npm run stop_node && npm run start_mongo && npm run start_node"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "angular": "^1.5.6",
    "body-parser": "1.15.2",
    "mongoose": "4.4.20"
  },
  "devDependencies": {
    "fly": "^1.3.1",
    "webpack": "^1.13.1",
    "mocha": "^2.5.3"
  }
}
